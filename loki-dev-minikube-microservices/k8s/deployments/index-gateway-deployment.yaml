apiVersion: apps/v1
kind: Deployment
metadata:
  name: loki-index-gateway
  namespace: loki
spec:
  replicas: 1
  selector:
    matchLabels:
      app: loki-index-gateway
  template:
    metadata:
      labels:
        app: loki-index-gateway
    spec:
      securityContext:
        fsGroup: 10001
      initContainers:
      - name: init-permissions
        image: busybox:1.35
        command: ['sh', '-c', 'chown -R 10001:10001 /loki']
        volumeMounts:
        - name: index-cache
          mountPath: /loki
      containers:
      - name: loki-index-gateway
        image: grafana/loki:3.5.5
        args:
        - -config.file=/etc/loki/index-gateway.yaml
        - -target=index-gateway
        ports:
        - containerPort: 3100
        - containerPort: 9095
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "200m"
            memory: "512Mi"
        volumeMounts:
        - name: config-volume
          mountPath: /etc/loki
          readOnly: true
        - name: index-cache
          mountPath: /loki
        securityContext:
          runAsNonRoot: true
          runAsUser: 10001
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
        env:
        - name: HOSTNAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: MINIO_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: minio-creds
              key: accesskey
        - name: MINIO_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: minio-creds
              key: secretkey
      volumes:
      - name: config-volume
        configMap:
          name: index-gateway-config
      - name: index-cache
        persistentVolumeClaim:
          claimName: index-cache