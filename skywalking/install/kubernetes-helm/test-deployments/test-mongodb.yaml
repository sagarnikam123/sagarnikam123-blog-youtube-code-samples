# =============================================================================
# Test MongoDB Deployment for Database Monitoring
# =============================================================================
# Deploys: MongoDB + mongodb-exporter
# Tests: Database → MongoDB menu in SkyWalking
#
# Usage:
#   kubectl apply -f test-mongodb.yaml -n skywalking
#   kubectl exec -it deploy/test-mongodb -c mongodb -- mongosh
# =============================================================================

---
apiVersion: v1
kind: Secret
metadata:
  name: test-mongodb-secret
  labels:
    app: test-mongodb
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: "admin"
  MONGO_INITDB_ROOT_PASSWORD: "test123"
  # Exporter connection string
  MONGODB_URI: "mongodb://admin:test123@localhost:27017/admin?authSource=admin"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-mongodb
  labels:
    app: test-mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-mongodb
  template:
    metadata:
      labels:
        app: test-mongodb
    spec:
      containers:
        # MongoDB Server
        - name: mongodb
          image: mongo:7
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: test-mongodb-secret
                  key: MONGO_INITDB_ROOT_USERNAME
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: test-mongodb-secret
                  key: MONGO_INITDB_ROOT_PASSWORD
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi

        # MongoDB Exporter (sidecar)
        - name: mongodb-exporter
          image: percona/mongodb_exporter:0.43.1
          ports:
            - name: metrics
              containerPort: 9216
          env:
            - name: MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: test-mongodb-secret
                  key: MONGODB_URI
          args:
            - --compatible-mode
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi

---
apiVersion: v1
kind: Service
metadata:
  name: test-mongodb
  labels:
    app: test-mongodb
spec:
  type: ClusterIP
  ports:
    - name: mongodb
      port: 27017
    - name: metrics
      port: 9216
  selector:
    app: test-mongodb
