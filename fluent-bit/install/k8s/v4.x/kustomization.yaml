apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fluent-bit

resources:
  - rbac.yaml
  - daemonset.yaml

configMapGenerator:
  - name: fluent-bit-config
    files:
      - fluent-bit.yaml
